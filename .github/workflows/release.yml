name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  MIX_ENV: prod
  OTP_VERSION: "27.3"
  ELIXIR_VERSION: "1.18.3"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: elixir

    steps:
      - uses: actions/checkout@v4

      - name: Install Erlang & Elixir
        uses: erlef/setup-beam@v1
        with:
          otp-version: ${{ env.OTP_VERSION }}
          elixir-version: ${{ env.ELIXIR_VERSION }}

      - name: Cache deps
        uses: actions/cache@v4
        with:
          path: |
            elixir/deps
            elixir/_build
          key: linux-amd64-mix-prod-${{ hashFiles('elixir/mix.lock') }}
          restore-keys: |
            linux-amd64-mix-prod-

      - name: Install deps
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get --only prod

      - name: Compile
        run: mix compile

      - name: Build release
        run: mix release karkhana

      - name: Package tarball
        run: |
          cd _build/prod/rel
          tar czf karkhana-${{ github.ref_name }}-linux-amd64.tar.gz karkhana/

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: elixir/_build/prod/rel/karkhana-${{ github.ref_name }}-linux-amd64.tar.gz
